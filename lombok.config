# @AllArgsConstructor 才会生效，在构造函数添加 @ConstructorProperties({"addressLine1", "addressLine2"})，用于jackson反序列化
lombok.anyConstructor.addConstructorProperties=true
lombok.addLombokGeneratedAnnotation = true
# easy-excel 不兼容 lombok.accessors.chain=true